<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- ✅ Required for video calls -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- For older android -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <!-- ✅ Required on Android 13+ (API 33+) to show any notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- ✅ Required for notification vibration -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- ✅ Required for flutter_local_notifications to wake device for heads-up -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- ✅ Required to show full-screen call notifications on locked screen -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>

    <!-- ✅ Required for flutter_callkit_incoming foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL"/>
    <!-- ✅ Required on Android 14+ (API 34+): service starts with FGS type "microphone" -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <!-- ✅ Required on Android 14+ (API 34+): FGS type "camera" for video calls -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS"/>

    <application
        android:label="Symmetry CareGiver"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">

        <!-- ✅ Google Maps API Key MUST be inside <application> -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCw6mXOPCtbKn5i0bWcAcnfXCkb0y5G7Lg" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Flutter normal theme -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter embedding -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ✅ Custom FCM service: intercepts ALL FCM messages via handleIntent() before
             Android decides to show a system notification. Priority 1 ensures it wins
             over FlutterFirebaseMessagingService (priority 0) from the firebase_messaging plugin.
             For CALL_INCOMING / CALL_ENDED / etc., we show the callkit UI and do NOT call super,
             preventing any plain system notification. For everything else we call super so Flutter
             background handler and chat notifications work normally. -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter android:priority="1">
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <!-- ✅ Override flutter_callkit_incoming service: use phoneCall|microphone|camera
             FGS type so accepting an audio or video call doesn't crash on Android 14+.
             tools:replace ensures our type wins over the plugin's. -->
        <service
            android:name="com.hiennv.flutter_callkit_incoming.CallkitNotificationService"
            android:foregroundServiceType="phoneCall|microphone|camera"
            tools:replace="android:foregroundServiceType"/>

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>