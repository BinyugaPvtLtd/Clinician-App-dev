enum AnswerType {
  checkbox,
  option,
  textBox,
  textArea,
  none,
  date,
  info,
  dropdown,
  radio,
  button,
  upload,
  static,
  actionButton,
  time,

  // START For Wound Screen Only
  addWoundImage,
  foamStatic,
  divider,
  // END For Wound Screen Only
}

enum QuestionType { comprehensive, nonComprehensive, none, both }

enum OptionsAlignment { horizontal, vertical, none }
